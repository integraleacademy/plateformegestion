export function isOutsideCanvas(element,canvas){const e=element.getBoundingClientRect(),c=canvas.getBoundingClientRect(),t=1;return e.left<c.left-t||e.top<c.top-t||e.right>c.right+t||e.bottom>c.bottom+t;}
function overflowMessage(el,canvas){const e=el.getBoundingClientRect(),c=canvas.getBoundingClientRect();const name=el.dataset.elementName||el.dataset.contentKey||'Élément';const bottom=Math.max(0,Math.round(e.bottom-c.bottom)),right=Math.max(0,Math.round(e.right-c.right)),top=Math.max(0,Math.round(c.top-e.top)),left=Math.max(0,Math.round(c.left-e.left));const px=Math.max(bottom,right,top,left);const side=bottom?'en bas':right?'à droite':top?'en haut':'à gauche';return `Le bloc « ${name} » dépasse de ${px} px ${side}.`;}
export function validateStudioSlide(slideNode,options={}){const blockingErrors=[],warnings=[],info=[];const canvas=slideNode.matches?.('.social-studio-slide')?slideNode:slideNode.querySelector('.social-studio-slide')||slideNode;const w=parseInt(canvas.style.width||canvas.dataset.width||canvas.offsetWidth,10),h=parseInt(canvas.style.height||canvas.dataset.height||canvas.offsetHeight,10);if(!w||!h)blockingErrors.push({message:'Dimensions de la scène invalides.',element:null});if(!canvas.dataset.renderedTemplateId)blockingErrors.push({message:'Renderer absent ou template non rendu.',element:canvas});const logo=canvas.querySelector('.studio-brand-footer__logo');if(!logo)blockingErrors.push({message:'Logo officiel obligatoire absent.',element:canvas});else if(!/logo-integrale\.png(?:$|[?#])/.test(logo.getAttribute('src')||''))blockingErrors.push({message:'Le logo officiel static/img/logo-integrale.png doit être utilisé.',element:logo});if(!canvas.textContent.includes('Faites le premier pas vers votre futur métier'))blockingErrors.push({message:'Slogan obligatoire absent.',element:canvas});canvas.querySelectorAll('[data-exportable="true"]').forEach(el=>{if(el.closest('[data-editor-only]'))return;if(isOutsideCanvas(el,canvas))blockingErrors.push({message:overflowMessage(el,canvas),element:el});});const safe=70;canvas.querySelectorAll('[data-exportable="true"]').forEach(el=>{const r=el.getBoundingClientRect(),c=canvas.getBoundingClientRect();if(!isOutsideCanvas(el,canvas)&&(r.left<c.left+safe||r.top<c.top+safe||r.right>c.right-safe||r.bottom>c.bottom-safe))warnings.push({message:`Le bloc « ${el.dataset.elementName||'élément'} » est proche de la marge de sécurité.`,element:el});});if(canvas.textContent.match(/(Lieu|Début|Durée|Financement|Places)(?=\S)/))warnings.push({message:'Texte potentiellement concaténé détecté.',element:canvas});if(!blockingErrors.length)info.push({message:`Dimensions : ${w} × ${h}`},{message:'Logo officiel chargé'},{message:'Slogan présent'},{message:'Footer libre'},{message:'Aucun chevauchement détecté'},{message:'Densité correcte'});return {blockingErrors,warnings,info};}
export function validateProject(project){const warnings=[];const slide=project.slides[project.activeSlideIndex],c=slide.content;if((c.title||'').length>90)warnings.push({type:'warning',message:`Titre trop long : réduisez-le de ${c.title.length-90} caractères.`});if(c._autoFormation&&c._autoFormation!==project.formation)warnings.push({type:'warning',message:`Le contenu actuel semble concerner ${c._autoFormation}, alors que la formation sélectionnée est ${project.formation}.`});return warnings;}
export function validateRendered(root){const r=validateStudioSlide(root);return [...r.blockingErrors.map(x=>({type:'error',message:x.message,element:x.element})),...r.warnings.map(x=>({type:'warning',message:x.message,element:x.element}))];}
