export async function exportCanvas(canvas,{type='png',scale=1,quality=.92,name='visuel'}={}){await document.fonts?.ready;canvas.discardActiveObject();canvas.requestRenderAll();const data=canvas.toDataURL({format:type,multiplier:Number(scale),quality:Number(quality),enableRetinaScaling:false});const a=document.createElement('a');a.href=data;a.download=`${slug(name)}-${new Date().toISOString().slice(0,10)}.${type==='jpeg'?'jpg':type}`;a.click()}
const slug=s=>String(s).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'').slice(0,80)||'visuel';
