export function sanitizeAiPayload(payload){const p=payload||{};const slide=(p.slides&&p.slides[0])||{};const c=slide.content||p.content||{};return {projectTitle:String(p.projectTitle||'Projet studio').slice(0,80),formation:['A3P','APS','SSIAP','DIRIGEANT','VTC'].includes(p.formation)?p.formation:'APS',content:{eyebrow:String(c.eyebrow||'FORMATION').slice(0,40),title:String(c.title||'Devenez agent de sécurité dès septembre.').slice(0,90),highlightedText:String(c.highlightedText||'').slice(0,60),introduction:String(c.introduction||'').slice(0,180),badges:(c.badges||[]).map(x=>String(x).slice(0,28)).slice(0,5),stats:(c.stats||[]).map(s=>({label:String(s.label||'Info').slice(0,35),value:String(s.value||'').slice(0,35),icon:String(s.icon||'info')})).slice(0,4),cta:String(c.cta||'Demander les informations').slice(0,45)}};}
