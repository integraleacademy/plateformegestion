export function renderLayers(canvas,root){root.replaceChildren(...canvas.getObjects().slice().reverse().map(object=>{const row=document.createElement('div');row.className='layer'+(canvas.getActiveObjects().includes(object)?' active':'');row.draggable=true;row.dataset.id=object.id;row.innerHTML=`<span>${object.type==='textbox'?'T':'◇'}</span><b>${escapeHtml(object.name||object.type)}</b><button data-visible>${object.visible===false?'○':'◉'}</button><button data-lock>${object.locked?'🔒':'🔓'}</button>`;row.onclick=e=>{if(e.target.closest('[data-visible]'))object.set('visible',!object.visible);else if(e.target.closest('[data-lock]')){object.locked=!object.locked;object.set({selectable:!object.locked,evented:!object.locked})}else canvas.setActiveObject(object);canvas.requestRenderAll();renderLayers(canvas,root)};row.ondragstart=e=>e.dataTransfer.setData('text/plain',object.id);row.ondragover=e=>e.preventDefault();row.ondrop=e=>{e.preventDefault();const moved=canvas.getObjects().find(x=>x.id===e.dataTransfer.getData('text/plain'));if(moved&&moved!==object){canvas.moveObjectTo(moved,canvas.getObjects().indexOf(object));canvas.requestRenderAll();canvas.fire('object:modified',{target:moved})}};return row}))}
function escapeHtml(s){const d=document.createElement('div');d.textContent=s;return d.innerHTML}
