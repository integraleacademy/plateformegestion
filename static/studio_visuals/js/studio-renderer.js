const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function applyThemeVars(node,theme){Object.entries(theme).forEach(([k,v])=>{if(typeof v==='string')node.style.setProperty('--theme-'+k.replace(/[A-Z]/g,m=>'-'+m.toLowerCase()),v)});node.style.setProperty('--studio-background',theme.background||theme.bgLight||'#fff')}
export function highlightTitle(title,frags){let parts=[esc(title)];const arr=(Array.isArray(frags)?frags:String(frags||'').split(/[;,\n]/)).map(x=>x.trim()).filter(Boolean);for(const f of arr){const re=new RegExp('('+f.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')+')','iu');parts=parts.flatMap(p=>p.includes('<span')?[p]:p.split(re).map(x=>re.test(x)?`<span class="title-highlight">${esc(x)}</span>`:esc(x)));}return parts.join('');}
function editable(key,html,tag='div',cls=''){return `<${tag} class="studio-editable-text ${cls}" data-element-id="${key}" data-content-key="${key}" tabindex="0">${html}</${tag}>`}
function renderLogo(slide,renderMode){if(slide.options?.showLogo===false)return '';const l=slide.logo;if(l?.url){const handle=slide._exporting?'':'<span class="studio-logo-resize" data-logo-resize="1"></span>';return `<div class="social-studio-slide__logo-box" data-logo-id="logo" data-element-id="logo" style="left:${Number(l.x||760)}px;top:${Number(l.y||72)}px;width:${Number(l.width||220)}px;opacity:${Number(l.opacity||1)}"><img class="social-studio-slide__logo social-studio-slide__logo-img" src="${esc(l.url)}" alt="Logo">${renderMode==='preview'?handle:''}</div>`}return editable('footer.centerName',esc(slide.content.footer?.centerName||'Intégrale Academy'),'div','social-studio-slide__logo')}
export function renderSlide(project,slide,renderMode='preview',registries={}){const theme=registries.themes?.[project.formation]||{};const c=slide.content;slide._exporting=renderMode==='export';const dims=project.format;const el=document.createElement('article');el.className=`social-studio-slide social-template social-template--${esc(slide.templateId)} social-studio-render-${renderMode}`;el.style.width=dims.width+'px';el.style.height=dims.height+'px';applyThemeVars(el,theme);const stats=(c.stats||[]).slice(0,4).map((s,i)=>`<div class="social-template__card" data-element-id="stat-${i}" tabindex="0"><div class="social-template__label">${esc(s.label)}</div><div class="studio-editable-text social-template__value" data-element-id="stats.${i}.value" data-content-key="stats.${i}.value" tabindex="0">${esc(s.value)}</div></div>`).join('');const badges=(c.badges||[]).slice(0,5).map((b,i)=>`<span class="studio-editable-text social-template__badge" data-element-id="badges.${i}" data-content-key="badges.${i}" tabindex="0">${esc(b)}</span>`).join('');const safe=renderMode==='preview'&&slide.options?.showSafeMargins!==false?'<div class="social-studio-safe"></div>':'';el.innerHTML=`<div class="social-studio-slide__inner"><div class="social-studio-slide__brand">${editable('eyebrow',esc(c.eyebrow),'div','social-studio-slide__eyebrow')}${renderLogo(slide,renderMode)}</div>${editable('title',highlightTitle(c.title,c.highlightedText),'h1','social-visual-title')} ${editable('introduction',esc(c.introduction),'p','social-visual-intro')}<section class="social-template__panel"><div class="social-template__badges">${badges}</div><div class="social-template__cards">${stats}</div>${editable('cta',esc(c.cta||''),'p','social-template__cta')}</section><footer class="social-studio-slide__footer"><span>${editable('footer.website',esc(c.footer?.website||'www.integraleacademy.com'),'span')} · ${editable('footer.phone',esc(c.footer?.phone||'04 22 47 07 68'),'span')}</span><span>${project.activeSlideIndex+1}/${project.slides.length}</span></footer>${(slide.elements||[]).map(x=>`<div class="studio-added-icon" style="left:${x.x}px;top:${x.y}px">${esc(x.label)}</div>`).join('')}${safe}</div>`;return el;}
